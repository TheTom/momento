"""Momento CLI — trust anchors for developers."""


def main() -> None:
    """CLI entry point.

    Commands: status, last, save, log, undo, inspect, prune,
    ingest, search, debug-restore.
    """
    raise NotImplementedError("cli.main")
